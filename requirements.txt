# Byteme MBA System — Python Dependencies
# Compatible with Python 3.10, 3.11, 3.12, 3.13

fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6
aiofiles>=0.23.0
pydantic>=2.0.0

# NOTE: FP-Growth, scoring, and CSV parsing are all pure Python.
# No numpy, pandas, or C compiler required.
